import 'package:flutter/material.dart';
import 'package:test_app/screens/rooms/components/body.dart';

class JoinRoom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Body(),
    );
  }
}
